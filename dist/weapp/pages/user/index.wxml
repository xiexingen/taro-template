<block wx:if="{{$taroCompReady}}">
    <basic-page>
        <block>
            <block wx:if="{{globalData.user}}">
                <view class="{{styles.header}}">
                    <at-avatar compid="{{$compid__417}}"></at-avatar>
                    <view class="{{styles.nickName}}">{{nickName}}</view>
                </view>
            </block>
            <block wx:else>
                <view bindtap="anonymousFunc0" class="{{styles.header}}">点击登录</view>
            </block>
        </block>
        <view class="{{styles.operator}}">
            <view class="{{styles.item}}">
                <at-button compid="{{$compid__418}}">我发起的</at-button>
            </view>
            <view class="{{styles.item}}">
                <at-button compid="{{$compid__419}}">我参与的</at-button>
            </view>
        </view>
        <at-list>
            <at-list-item wx:key="id" wx:for="{{loopArray29}}" wx:for-item="item" wx:for-index="__index3" compid="{{item.$compid__416}}"></at-list-item>
        </at-list>
    </basic-page>
</block>